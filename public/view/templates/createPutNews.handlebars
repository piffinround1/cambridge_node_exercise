 	<div class="well">
	                <h3 class="text-info">{{title}} ({{action}})</h3>
	                    <p>
	                        <strong>title</strong>: <input type='text' value='{{title}}' id='newsTitle'><br>
	                        
	                        <strong>description</strong>: <input type='text' value='{{description}}' id='newsDesc'><br>
	                        
	                    </p>

	                    <a href="#" class="btn btn-info" id='doAction' identifier='{{action}}' newsId = '{{_id}}'>{{action}}</a>
	                    <a href="#" class="btn btn-info" id ='back' identifier='{{action}}'>Back</a>
	</div>